---
apiVersion: v1
kind: Service
metadata:
  name: changedetection
  namespace: default
  labels:
    app: changedetection
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: changedetection
