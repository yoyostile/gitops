---
# Declare client_secret because Mealie reads the same substituted value.
apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprint-mealie
  namespace: default
data:
  mealie.yaml: |
    version: 1
    metadata:
      name: Mealie (gitops)
      labels:
        blueprints.goauthentik.io/instantiate: "true"
    entries:
      - model: authentik_providers_oauth2.oauth2provider
        state: present
        identifiers:
          name: Provider for Mealie
        attrs:
          access_code_validity: minutes=1
          access_token_validity: minutes=5
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          client_id: "${MEALIE_OAUTH_CLIENT_ID}"
          client_secret: "${MEALIE_OAUTH_CLIENT_SECRET}"
          client_type: confidential
          grant_types:
            - authorization_code
            - refresh_token
          include_claims_in_id_token: true
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          issuer_mode: per_provider
          logout_method: backchannel
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [name, 'authentik default OAuth Mapping: OpenID ''offline_access''']]
            - !Find [authentik_providers_oauth2.scopemapping, [name, 'authentik default OAuth Mapping: OpenID ''openid''']]
            - !Find [authentik_providers_oauth2.scopemapping, [name, 'authentik default OAuth Mapping: OpenID ''email''']]
            - !Find [authentik_providers_oauth2.scopemapping, [name, 'authentik default OAuth Mapping: OpenID ''profile''']]
          redirect_uris:
            - matching_mode: strict
              redirect_uri_type: authorization
              url: https://mealie.${SECRET_LAB_DOMAIN}/login
            - matching_mode: strict
              redirect_uri_type: authorization
              url: https://mealie.${SECRET_LAB_DOMAIN}/login?direct=1
          refresh_token_threshold: seconds=0
          refresh_token_validity: days=30
          signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
          sub_mode: hashed_user_id
      # Must match the slug in OIDC_CONFIGURATION_URL.
      - model: authentik_core.application
        state: present
        identifiers:
          slug: mealie
        attrs:
          meta_hide: false
          meta_launch_url: https://mealie.${SECRET_LAB_DOMAIN}
          name: Mealie
          open_in_new_tab: false
          policy_engine_mode: any
          provider: !Find [authentik_providers_oauth2.oauth2provider, [name, Provider for Mealie]]
      - model: authentik_policies.policybinding
        state: present
        identifiers:
          target: !Find [authentik_core.application, [slug, mealie]]
          order: 0
          group: !Find [authentik_core.group, [name, Mealie]]
        attrs:
          enabled: true
          negate: false
          failure_result: false
          timeout: 30
