---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: dawarich-db
  namespace: default
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: dawarich-db
  podMetricsEndpoints:
    - port: metrics
