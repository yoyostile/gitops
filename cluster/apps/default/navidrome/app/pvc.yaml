apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: navidrome
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: proxmox-data-ext4
  resources:
    requests:
      storage: 1Gi
