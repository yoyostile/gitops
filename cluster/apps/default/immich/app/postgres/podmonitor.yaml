---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: immich-postgres
  namespace: default
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: immich-postgres
  podMetricsEndpoints:
    - port: metrics
