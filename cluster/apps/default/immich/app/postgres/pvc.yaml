---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-cnpg-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 32Gi
  storageClassName: local-storage
  volumeName: immich-cnpg-pv
