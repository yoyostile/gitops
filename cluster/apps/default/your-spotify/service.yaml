apiVersion: v1
kind: Service
metadata:
  name: your-spotify
  namespace: default
spec:
  selector:
    app: your-spotify
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
    - protocol: TCP
      port: 3000
      targetPort: 3000
