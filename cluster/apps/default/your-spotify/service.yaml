apiVersion: v1
kind: Service
metadata:
  name: spotify-service
spec:
  selector:
    app: spotify
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
    - protocol: TCP
      port: 3000
      targetPort: 3000
