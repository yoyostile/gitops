---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: echo
  name: echo
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: echo
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: echo
    spec:
      containers:
        - image: ghcr.io/mendhak/http-https-echo:41
          imagePullPolicy: Always
          name: echo
          env:
            - name: HTTP_PORT
              value: "8080"
          ports:
            - containerPort: 8080
              protocol: TCP
          # Sized from 30 days of observed usage.
          resources:
            requests:
              cpu: 10m
              memory: 96Mi
            limits:
              memory: 192Mi
      restartPolicy: Always
