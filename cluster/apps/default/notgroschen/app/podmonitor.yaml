---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: psql-notgroschen
  namespace: default
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: psql-notgroschen
  podMetricsEndpoints:
    - port: metrics
