---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photo-stream
  namespace: default
  labels:
    velero.io/exclude-from-backup: "true"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "nfs-client"
  resources:
    requests:
      storage: 5G
