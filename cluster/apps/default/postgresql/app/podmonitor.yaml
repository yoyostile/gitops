---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: psql-cluster
  namespace: default
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: psql-cluster
  podMetricsEndpoints:
    - port: metrics
