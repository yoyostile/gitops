---
apiVersion: k8s.mariadb.com/v1alpha1
kind: Database
metadata:
  name: librenms
  namespace: default
spec:
  mariaDbRef:
    name: mariadb-galera
  characterSet: utf8
  collate: utf8_general_ci
  cleanupPolicy: Skip
  requeueInterval: 30s
  retryInterval: 5s
---
apiVersion: k8s.mariadb.com/v1alpha1
kind: User
metadata:
  name: librenms
  namespace: default
spec:
  mariaDbRef:
    name: mariadb-galera
  passwordSecretKeyRef:
    name: mariadb-galera
    key: librenms-password
  host: "%"
  cleanupPolicy: Skip
  requeueInterval: 30s
  retryInterval: 5s
  maxUserConnections: 150
---
apiVersion: k8s.mariadb.com/v1alpha1
kind: Grant
metadata:
  name: librenms
  namespace: default
spec:
  mariaDbRef:
    name: mariadb-galera
  privileges:
    - "ALL PRIVILEGES"
  database: librenms
  table: "*"
  username: librenms
  grantOption: true
  host: "%"
  cleanupPolicy: Skip
  requeueInterval: 30s
  retryInterval: 5s
