---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: seerr
  namespace: media
spec:
  replicas: 1
  # config PVC is ReadWriteOnce (ceph-rbd); Recreate avoids Multi-Attach
  # deadlock when a new pod surges before the old one releases the volume
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: seerr
  template:
    metadata:
      labels:
        app.kubernetes.io/name: seerr
    spec:
      nodeSelector:
        topology.kubernetes.io/region: lab
      securityContext:
        fsGroup: 1000
      containers:
        - name: seerr
          image: ghcr.io/seerr-team/seerr:latest
          env:
            - name: TZ
              value: "Europe/Berlin"
            - name: NODE_OPTIONS
              value: "--require /app/dns-ipv4.cjs"
          ports:
            - name: http
              containerPort: 5055
          volumeMounts:
            - name: config
              mountPath: /app/config
            - name: dns-fix
              mountPath: /app/dns-ipv4.cjs
              subPath: dns-ipv4.cjs
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 250Mi
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: seerr-config-ceph
        - name: dns-fix
          configMap:
            name: seerr-dns-fix
