---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seerr-config-ceph
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd
  resources:
    requests:
      storage: 1Gi
