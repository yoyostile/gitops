---
apiVersion: v1
kind: Service
metadata:
  name: tautulli
  namespace: media
spec:
  ports:
    - port: 8181
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: tautulli
