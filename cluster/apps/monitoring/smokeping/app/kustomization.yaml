---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Distinct names keep Helm's uninstall from deleting the replacement resources.
resources:
  - pvc.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
