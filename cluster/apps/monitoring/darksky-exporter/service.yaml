apiVersion: v1
kind: Service
metadata:
  name: darksky-exporter
  namespace: monitoring
spec:
  type: ClusterIP
  ports:
    - port: 9091
      targetPort: 9091
      protocol: TCP
      name: metrics
  selector:
    app: darksky-exporter
