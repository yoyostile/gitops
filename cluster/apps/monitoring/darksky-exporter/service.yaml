apiVersion: v1
kind: Service
metadata:
  name: darksky-exporter
  namespace: monitoring
  labels:
    app: darksky-exporter
spec:
  type: ClusterIP
  ports:
    - port: 9091
      targetPort: 9091
      protocol: TCP
      name: metrics
  selector:
    app: darksky-exporter
