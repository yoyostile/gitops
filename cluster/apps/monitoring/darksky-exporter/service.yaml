apiVersion: v1
kind: Service
metadata:
  name: darksky-exporter
  namespace: monitoring
spec:
  type: ClusterIP
  ports:
    - port: 8086
      targetPort: 8086
      protocol: TCP
      name: metrics
  selector:
    app: darksky-exporter
